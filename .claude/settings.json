{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'if git diff --quiet && git diff --cached --quiet; then exit 0; fi; git add -A && git commit -m \"Auto-commit: $(date +%Y-%m-%d\\ %H:%M:%S)\" --no-verify 2>/dev/null || true'"
          }
        ]
      }
    ]
  }
}
