Run the full check suite (typecheck, lint, test) and fix any issues found.

1. Run `npm run check`
2. If there are type errors, fix them
3. If there are lint errors, run `npm run lint:fix` first, then fix remaining issues
4. If tests fail, investigate and fix
5. Re-run `npm run check` to confirm everything passes
